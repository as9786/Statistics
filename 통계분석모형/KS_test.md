# Kolmogorov-Smirnov test(K-S test, 콜모고로프-스미르노프 검정)
- 두 집단의 누적 분포의 차이를 나타내는 지표
- 두 표본 집단이 같은 확률 분포에서 추출되었는지 판단
- 비모수 검정(분포 가정 X, data 자체로 판단)
- 연속 변수(CDF)와 이산 변수(EDF) 모두 사용 가능
- 두 분포의 최대 차이 -> KS statistic
- $D = max_x|F_1(x) - F_2(x)|$
- 두 곡선 사이의 가장 큰 거리

